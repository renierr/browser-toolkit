{
  "name": "browser-toolkit-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run --watch server.ts"
  },
  "dependencies": {
    "hono": "^4.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
